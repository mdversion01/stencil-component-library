import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Intro/Getting Started" />

# Stencil Component Library — Storybook

This Storybook uses the **Web Components** renderer with Vite.
Your Stencil build auto-defines all custom elements when `dist/components` is imported.

- Run `npm run storybook` for local docs.
- Components demonstrate basic attribute mapping via Controls.
