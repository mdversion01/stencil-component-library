import { Meta, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';
import * as AppWrapperStories from './app-wrapper.stories.js';
import { reactExample, vueExample, angularExample, svelteExample, svelteKitExample } from './app-wrapper.examples.js';

<Meta of={AppWrapperStories} />

# App Wrapper

<Description of={AppWrapperStories} />

## HTML

```html
<app-wrapper class-names="p-4 bg-light rounded">
  <h2 class="mb-2">Wrapper Example</h2>
  <p class="mb-3">Content placed inside the component is rendered through the default slot.</p>

  <div style="display:flex; gap:.5rem; flex-wrap:wrap">
    <button-component btn-text="Primary" variant="primary" size="sm"></button-component>
    <button-component btn-text="Secondary" variant="secondary" size="sm"></button-component>
  </div>
</app-wrapper>
```

## React

<Source code={reactExample} language="tsx" />
## Vue
<Source code={vueExample} language="vue" />
## Angular
<Source code={angularExample} language="ts" />
## Svelte
<Source code={svelteExample} language="svelte" />
## SvelteKit
<Source code={svelteKitExample} language="svelte" />

{/* Show a live preview + controls table */}

<Primary />
<Controls />
<Stories />
