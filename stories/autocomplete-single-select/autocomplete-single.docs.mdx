import { Meta, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';
import * as AutocompleteSingleStories from './autocomplete-single.stories.js';
import { reactExample, vueExample, angularExample, svelteExample, svelteKitExample } from './autocomplete-single.examples.js';

<Meta of={AutocompleteSingleStories} />

# Autocomplete Single

<Description of={AutocompleteSingleStories} />

## HTML

```html
<autocomplete-single
  input-id="autocomplete-single-example"
  label="Favorite fruit"
  placeholder="Type to search"
></autocomplete-single>

<script>
  const el = document.querySelector('autocomplete-single')

  el.options = [
    'Apple',
    'Banana',
    'Orange',
    'Mango',
    'Blueberry',
    'Strawberry'
  ]

  el.value = 'Apple'
  el.autoSort = true

  el.addEventListener('itemSelect', (event) => {
    console.log('itemSelect:', event.detail)
  })

  el.addEventListener('valueChange', (event) => {
    console.log('valueChange:', event.detail)
  })
</script>
```

## React
<Source code={reactExample} language="tsx" />
## Vue
<Source code={vueExample} language="vue" />
## Angular
<Source code={angularExample} language="ts" />
## Svelte
<Source code={svelteExample} language="ts" />
## Svelte Kit
<Source code={svelteKitExample} language="ts" />

{/* Show a live preview + controls table */}
<Primary />
<Controls />
<Stories />
