import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Source } from '@storybook/addon-docs/blocks';
import * as TextareaStories from './textarea-component.stories.js';
import {
  htmlExample,
  reactExample,
  vueExample,
  angularExample,
  svelteExample,
  svelteKitExample,
} from './textarea-component.examples';

<Meta of={TextareaStories} />

<Title />
<Subtitle>Multiline form textarea with Bootstrap-style layout, validation, counter, and form events.</Subtitle>

## HTML

Use the component directly in HTML. The counter is shown automatically when `max-length` is provided.


<Source dark code={htmlExample} language="html" />

## React

<Source dark code={reactExample} language="tsx" />

## Vue

<Source dark code={vueExample} language="html" />

## Angular

<Source dark code={angularExample} language="ts" />

## Svelte

<Source dark code={svelteExample} language="html" />

## SvelteKit

<Source dark code={svelteKitExample} language="html" />
{/* Show a live preview + controls table */}
<Primary />
<Controls />
<Stories />
