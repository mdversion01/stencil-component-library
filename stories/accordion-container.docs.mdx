import { Meta, Title, Subtitle, Description, Primary, Controls, Stories } from '@storybook/blocks';
import * as AccordionContainerStories from './accordion-container.stories';

<Meta of={AccordionContainerStories} />

<Title />
<Subtitle>An Accordion is a UI pattern that lets users expand and collapse sections of related content.</Subtitle>

<Primary />

<Description of={AccordionContainerStories} />

{/* Show a live preview + controls table */}
<Controls />
<Stories />
