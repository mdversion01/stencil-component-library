import { Meta, Description, Primary, Controls, Stories, Source } from '@storybook/addon-docs/blocks';
import * as PlumageTimepickerStories from './plumage-timepicker-component.stories.js';
import { reactExample, vueExample, angularExample, svelteExample, svelteKitExample } from './plumage-timepicker-component.examples.js';

<Meta of={PlumageTimepickerStories} />

# Plumage Timepicker

<Description of={PlumageTimepickerStories} />

## HTML

```html
<plumage-timepicker-component
  show-label
  label-text="Meeting time"
  input-id="time-example"
  input-name="meetingTime"
  value="00:00:00"
  is-twenty-four-hour-format="true"
></plumage-timepicker-component>

<script>
  const el = document.querySelector('plumage-timepicker-component');

  el.addEventListener('timeChange', (event) => {
    console.log('timeChange:', event.detail);
  });

  el.addEventListener('timeInput', (event) => {
    console.log('timeInput:', event.detail);
  });
</script>
```

## React
<Source dark code={reactExample} language="tsx" />
## Vue
<Source dark code={vueExample} language="vue" />
## Angular
<Source dark code={angularExample} language="ts" />
## Svelte
<Source dark code={svelteExample} language="svelte" />
## SvelteKit
<Source dark code={svelteKitExample} language="svelte" />

{/* Show a live preview + controls table */}
<Primary />
<Controls />
<Stories />
