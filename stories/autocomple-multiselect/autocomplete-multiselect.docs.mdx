import { Meta, Description, Primary, Controls, Stories, Source } from '@storybook/addon-docs/blocks';
import * as AutocompleteMultiselectStories from './autocomplete-multiselect.stories.js';
import { reactExample, vueExample, angularExample, svelteExample, svelteKitExample } from './autocomplete-multiselect.examples.js';

<Meta of={AutocompleteMultiselectStories} />

# Autocomplete Multiselect

<Description of={AutocompleteMultiselectStories} />

## HTML

```html
<autocomplete-multiselect
  input-id="acm-example"
  label="Favorite fruits"
  placeholder="Type to search"
  editable
  add-btn
  badge-variant="primary"
></autocomplete-multiselect>

<script>
  const el = document.querySelector('autocomplete-multiselect')

  el.options = [
    'Apple',
    'Banana',
    'Orange',
    'Mango',
    'Blueberry',
    'Strawberry'
  ]

  el.value = ['Apple', 'Mango']

  el.addEventListener('multiSelectChange', (event) => {
    console.log('multiSelectChange:', event.detail)
  })

  el.addEventListener('optionsChange', (event) => {
    console.log('optionsChange:', event.detail)
  })
</script>
```
## React
<Source dark code={reactExample} language="tsx" />
## Vue
<Source dark code={vueExample} language="vue" />
## Angular
<Source dark code={angularExample} language="ts" />
## Svelte
<Source dark code={svelteExample} language="ts" />
## Svelte Kit
<Source dark code={svelteKitExample} language="ts" />

{/* Show a live preview + controls table */}
<Primary />
<Controls />
<Stories />
