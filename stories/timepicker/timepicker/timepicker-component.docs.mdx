import { Meta, Description, Primary, Controls, Stories, Source } from '@storybook/blocks';
import * as TimepickerStories from './timepicker-component.stories.js';
import { reactExample, vueExample, angularExample, svelteExample, svelteKitExample } from './timepicker-component.examples.js';

<Meta of={TimepickerStories} />

# Timepicker Component

<Description of={TimepickerStories} />

## HTML

```html
<timepicker-component
  show-label
  label-text="Meeting time"
  input-id="time-example"
  input-name="meetingTime"
  value="13:05:09"
  is-twenty-four-hour-format
></timepicker-component>

<script>
  const el = document.querySelector('timepicker-component');

  el.addEventListener('timeChange', (event) => {
    console.log('timeChange:', event.detail);
  });

  el.addEventListener('timeInput', (event) => {
    console.log('timeInput:', event.detail);
  });
</script>
```

## React
<Source code={reactExample} language="tsx" />
## Vue
<Source code={vueExample} language="vue" />
## Angular
<Source code={angularExample} language="ts" />
## Svelte
<Source code={svelteExample} language="svelte" />
## SvelteKit
<Source code={svelteKitExample} language="svelte" />

{/* Show a live preview + controls table */}
<Primary />
<Controls />
<Stories />
